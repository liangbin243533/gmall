spring:
  application:
    name: pms-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: SEATA_GROUP
        file-extension: yml
    alibaba:
      seata:
        tx-service-group: pms-service